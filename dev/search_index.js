var documenterSearchIndex = {"docs":
[{"category":"section","location":"references/#References","page":"References","text":"The algorithms in this package are implementations of methods from the following papers.\n\nRandomized range finder, SVD, and Hermitian eigendecomposition\n\nN. Halko, P. G. Martinsson, and J. A. Tropp, \"Finding Structure with Randomness: Probabilistic Algorithms for Constructing Approximate Matrix Decompositions\", SIAM Review 53(2), 217-288, 2011. arXiv:0909.4061.\n\nStochastic trace estimation: XTrace\n\nE. N. Epperly, J. A. Tropp, and R. J. Webber, \"XTrace: Making the Most of Every Sample in Stochastic Trace Estimation\", SIAM Journal on Matrix Analysis and Applications 45(1), 2024. arXiv:2301.07825.\n\nStochastic trace estimation: Girard-Hutchinson\n\nM. F. Hutchinson, \"A stochastic estimator of the trace of the influence matrix for Laplacian smoothing splines\", Communications in Statistics, Simulation and Computation 18(3), 1989.\nA. Girard, \"A fast Monte-Carlo cross-validation procedure for large least squares problems with noisy data\", Numerische Mathematik 56, 1989.","title":"References"},{"category":"section","location":"api/#API-Reference","page":"API","text":"","title":"API Reference"},{"category":"method","location":"api/#MatrixFreeRandomizedLinearAlgebra.reigen_hermitian-Tuple{Any, Int64}","page":"API","text":"reigen_hermitian(operator, num_components;\n                 num_oversamples=num_components,\n                 num_power_iterations=(num_components < 0.1 * minimum(size(operator)) ? 14 : 8),\n                 sample_vec=similar(operator, eltype(operator), 0), seed_Q=nothing)\n\nCompute a randomized eigendecomposition of a Hermitian matrix-like operator.\n\nThis routine finds approximate leading eigenvalues and eigenvectors of a Hermitian operator operator using randomized subspace iteration. It first builds an approximate invariant subspace via a randomized range finder and then computes the exact eigendecomposition of operator restricted to that subspace.\n\nArguments\n\noperator: Hermitian linear operator (self-adjoint with respect to the standard inner product), supporting size(operator) and operator * X.\nnum_components::Int: Number of leading eigenpairs to approximate.\n\nKeyword arguments\n\nnum_oversamples::Int = num_components: Oversampling parameter p; the sketch dimension is num_components + p.\nnum_power_iterations::Int: Number of power iterations used in the Hermitian range finder. Defaults to 14 for relatively small ranks (when num_components < 0.1 * min(size(operator))) and 8 otherwise. Larger values improve separation of clustered eigenvalues.\nsample_vec::AbstractVector: Prototype vector used for allocating random test matrices. Controls whether temporaries live on CPU or GPU.\nseed_Q = nothing: Optional warm-start basis for the range finder. If you already have an (approximately) orthonormal matrix whose columns span the invariant subspace of operator, pass it here to seed the subspace iteration instead of starting from pure Gaussian noise. This is useful when refining a previous solve or marching a parameter/time step. seed_Q does not have to be perfectly orthonormal (it is re-orthonormalized internally) and may have fewer columns than num_components + num_oversamples, in which case it is padded with random columns. With num_power_iterations = 0 the seed is used essentially as-is (up to re-orthonormalization).\n\nReturns\n\nAn Eigen object E such that\n\noperator * E.vectors ≈ E.vectors * Diagonal(E.values)\n\nwith length(E.values) == num_components (or fewer if the effective numerical rank is smaller). Eigenvalues are sorted in descending order.\n\nReferences\n\nN. Halko, P. G. Martinsson, and J. A. Tropp, \"Finding Structure with Randomness: Probabilistic Algorithms for Constructing Approximate Matrix Decompositions\", SIAM Review 53(2), 2011 (arXiv:0909.4061).\n\n\n\n\n\n","title":"MatrixFreeRandomizedLinearAlgebra.reigen_hermitian"},{"category":"method","location":"api/#MatrixFreeRandomizedLinearAlgebra.reigvals_hermitian-Tuple{Any, Int64}","page":"API","text":"reigvals_hermitian(operator, num_components;\n                  num_oversamples=num_components,\n                  num_power_iterations=(num_components < 0.1 * minimum(size(operator)) ? 14 : 8),\n                  sample_vec=similar(operator, eltype(operator), 0), seed_Q=nothing)\n\nCompute approximate leading eigenvalues of a Hermitian matrix-like operator.\n\nThis routine finds approximate leading eigenvalues of a Hermitian operator operator using randomized subspace iteration. It first builds an approximate invariant subspace via a randomized range finder and then computes the exact eigenvalues of operator restricted to that subspace.\n\nArguments\n\noperator: Hermitian linear operator (self-adjoint with respect to the   standard inner product), supporting size(operator) and operator * X.\nnum_components::Int: Number of leading eigenvalues to approximate.\n\nKeyword arguments\n\nnum_oversamples::Int = num_components: Oversampling parameter p; the sketch dimension is num_components + p.\nnum_power_iterations::Int:   Number of power iterations used in the Hermitian range finder. Defaults to 14   for relatively small ranks (when num_components < 0.1 * min(size(operator)))   and 8 otherwise. Larger values improve separation of clustered eigenvalues.\nsample_vec::AbstractVector:   Prototype vector used for allocating random test matrices. Controls whether   temporaries live on CPU or GPU.\nseed_Q = nothing:   Optional warm-start basis for the range finder. If you already have an   (approximately) orthonormal matrix whose columns span the invariant subspace   of operator, pass it here to seed the subspace iteration instead of   starting from pure Gaussian noise. It does not be perfectly orthonormal (it   is re-orthonormalized internally) and may have fewer columns than   num_components + num_oversamples, in which case it is padded with random   columns. With num_power_iterations = 0 the seed is used essentially as-is   (up to re-orthonormalization).\n\nReturns\n\nA vector of approximate leading eigenvalues evals such that\n\noperator * v ≈ evals[i] * v\n\nfor the corresponding eigenvector v (not returned here), with length(evals) == num_components (or fewer if the effective numerical rank is smaller). Eigenvalues are sorted in descending order.\n\nThis can be significantly cheaper than reigen_hermitian if only eigenvalues are needed.\n\n\n\n\n\n","title":"MatrixFreeRandomizedLinearAlgebra.reigvals_hermitian"},{"category":"method","location":"api/#MatrixFreeRandomizedLinearAlgebra.rsvd-Tuple{Any, Int64}","page":"API","text":"rsvd(operator, num_components; num_oversamples=num_components,\n     num_power_iterations=(num_components < 0.1 * minimum(size(operator)) ? 7 : 4),\n     sample_vec=similar(operator, eltype(operator), 0), seed_Q=nothing)\n\nCompute a randomized low-rank singular value decomposition (SVD) of a matrix-like linear operator.\n\nThis implements a standard randomized SVD with Gaussian test vectors, oversampling, and power iteration. It is intended for large, possibly matrix-free operators where forming a dense matrix is expensive.\n\nArguments\n\noperator: A linear operator acting like an AbstractMatrix, supporting size(operator), operator * X, and operator' * X. This may be a dense matrix, a sparse matrix, a GPU matrix (CuMatrix), or a matrix-free object (e.g. from LinearMaps.jl).\nnum_components::Int: Target rank k for the approximation.\n\nKeyword arguments\n\nnum_oversamples::Int = num_components: Number of oversampling vectors p. The effective sketch dimension is k + p. Larger oversampling improves accuracy at the cost of extra multiplies.\nnum_power_iterations::Int: Number of power-iteration refinement steps. If num_components is less than 10% of the smaller dimension of operator, the default is 7; otherwise 4. Increasing this improves spectral separation for slow-decaying singular values but increases cost by additional passes over operator and operator'.\nsample_vec::AbstractVector: Prototype vector used to control allocation of random test matrices. By default, similar(operator, eltype(operator), 0) is used so that temporary arrays are allocated on the same device/storage as operator. You can pass a vector living on a different device (e.g. a CuVector) to force all temporaries onto that device.\nseed_Q = nothing: Optional warm-start basis for the range finder. If you already have an (approximately) orthonormal matrix whose columns span the range of operator (column space), pass it here to seed the subspace iteration instead of starting from pure Gaussian noise. It need not be perfectly orthonormal (it is re-orthonormalized internally) and may have fewer columns than `num_components\nnum_oversamples`, in which case it is padded with random columns. With\nnum_power_iterations = 0 the seed is used essentially as-is (up to re-orthonormalization).  Seeding is not supported for tall operators (size(operator, 1) > size(operator, 2)), which are transposed internally. For tall operators, the user is expected to transpose the operator themselves and seed a basis for its (wide) range, or omit seed_Q.\n\nReturns\n\nA LinearAlgebra.SVD object svd such that\n\nsvd.U * Diagonal(svd.S) * svd.Vt ≈ operator\n\nwith length(svd.S) == num_components (or fewer is the effective numerical rank is smaller).\n\nReferences\n\nN. Halko, P. G. Martinsson, and J. A. Tropp, \"Finding Structure with Randomness: Probabilistic Algorithms for Constructing Approximate Matrix Decompositions\", SIAM Review 53(2), 2011 (arXiv:0909.4061).\n\n\n\n\n\n","title":"MatrixFreeRandomizedLinearAlgebra.rsvd"},{"category":"method","location":"api/#MatrixFreeRandomizedLinearAlgebra.rsvdvals-Tuple{Any, Int64}","page":"API","text":"rsvdvals(operator, num_components; num_oversamples=num_components,\n         num_power_iterations=(num_components < 0.1 * minimum(size(operator)) ? 7 : 4),\n         sample_vec=similar(operator, eltype(operator), 0), seed_Q=nothing)\n\nCompute the leading singualr values of a matrix-like poerator using randomized SVD techniques, without explcitly forming the singular vectors.\n\nThis has the same interface and algorithmic structure as rsvd, but only returns the approximate singualr values.\n\nArguments\n\noperator: A linear operator acting like an AbstractMatrix, supporting size(operator), operator * X, and operator' * X. This may be a dense matrix, a sparse matrix, a GPU matrix (CuMatrix), or a matrix-free object (e.g. from LinearMaps.jl).\nnum_components::Int: Target number of singular values k to compute.\n\nKeyword arguments\n\nnum_oversamples::Int = num_components: Number of oversampling vectors p. The effective sketch dimension is k + p. Larger oversampling improves accuracy at the cost of extra multiplies.\nnum_power_iterations::Int:   Number of power-iteration refinement steps. If num_components is less than   10% of the smaller dimension of operator, the default is 7; otherwise 4.   Increasing this improves spectral separation for slow-decaying singular   values but increases cost by additional passes over operator and   operator'.\nsample_vec::AbstractVector: Prototype vector used to control allocation of random test matrices. By default, similar(operator, eltype(operator), 0) is used so that temporary arrays are allocated on the same device/storage as operator. You can pass a vector living on a different device (e.g. a CuVector) to force all temporaries onto that device.\nseed_Q = nothing: Optional warm-start basis for the range finder. If you already have an (approximately) orthonormal matrix whose columns span the range of operator (column space), pass it here to seed the subspace iteration instead of starting from pure Gaussian noise. It need not be perfectly orthonormal (it is re-orthonormalized internally) and may have fewer columns than `num_components\nnum_oversamples`, in which case it is padded with random columns. With\nnum_power_iterations = 0 the seed is used essentially as-is.  Seeding is not supported for tall operators (size(operator, 1) > size(operator, 2)). For tall operators, the user is expected to transpose the operator themselves\n\nReturns\n\nA vector of length num_components (or fewer if the effective numerical rank is smaller) containing the leading singular values of operator.\n\nThis can be significantly cheaper (in memory and computation) to use than rsvd when only singular values are needed.\n\n\n\n\n\n","title":"MatrixFreeRandomizedLinearAlgebra.rsvdvals"},{"category":"function","location":"api/#MatrixFreeRandomizedLinearAlgebra.trace","page":"API","text":"trace(operator, num_samples; relative_tolerance=nothing, low_mem=false,\n      return_error=false, min_samples=30,\n      max_samples=(low_mem ? 4096 : min(size(operator, 1), 512)),\n      sample_vec=similar(operator, eltype(operator), 0))\n\nEstimate the trace of a square matrix-like linear operator using randomized (stochastic) trace estimation.\n\nTwo estimators are available. By default trace uses XTrace, the exchangeable estimator of Epperly, Tropp and Webber, which combines a low-rank sketch with a leave-one-out Hutchinson correction and so makes the most of every sample. When low_mem=true, trace falls back to the streaming Girard-Hutchinson estimator, which only ever holds one or two vectors at a time. Hutchinson is less accurate per matrix-vector product, but is the right choice when the operator is so large that even a small sketch (a handful of size(operator, 1)-length vectors) does not fit in memory.\n\nThe sample budget can be given two ways: a fixed num_samples, or a target relative_tolerance that the estimator refines towards adaptively. Provide exactly one of the two.\n\nArguments\n\noperator: A square linear operator acting like an AbstractMatrix, supporting size(operator), operator * X, and (for XTrace) operator' * X. This may be a dense matrix, a sparse matrix, a GPU matrix (CuMatrix), or a matrix-free object (e.g., from LinearMaps.jl).\nnum_samples::Int (optional, positional): Fixed sample budget. For Hutchinson this is the number of matrix-vector products. For XTrace this is the number of random test vectors m (which sets the memory cost); XTrace performs 2m matrix-vector products. Omit this and pass relative_tolerance instead to run adaptively.\n\nKeyword arguments\n\nrelative_tolerance::Real = nothing: Target relative error on the trace. When set (and num_samples is omitted), the estimator adds samples until its error estimate falls below relative_tolerance * abs(trace). For Hutchinson the stopping rule is a central-limit half-width (drawn after at least min_samples samples); for XTrace the sketch size is doubled until the jackknife error is small enough.\nlow_mem::Bool = false: If true, use the streaming Hutchinson estimator (minimal memory). If false, use XTrace.\nreturn_error::Bool = false: If true, return (value=t, error=e) where e is the estimated standard error (the jackknife error for XTrace, the sample standard error for Hutchinson). If false, return just the scalar estimate t.\nmin_samples::Int = 30: Hutchinson only. The number of samples drawn before the adaptive stopping rule is first checked, so the variance estimate has settled.\nmax_samples::Int: Cap on the number of samples (Hutchinson) or test vectors (XTrace) used by the adaptive modes. Defaults to 4096 for Hutchinson and min(size(operator, 1), 512) for XTrace.\nsample_vec::AbstractVector: Prototype vector used to control allocation of random test vectors. By default, similar(operator, eltype(operator), 0) is used so that temporary arrays are allocated on the same device/storage as operator. You can pass a vector living on a different device (e.g. a CuVector) to force all temporaries onto that device.\n\nReturns\n\nThe estimated trace (a scalar of eltype(operator)), or, if return_error=true, a named tuple (value, error) with the estimate and its estimated standard error.\n\nReferences\n\nE. N. Epperly, J. A. Tropp, and R. J. Webber, \"XTrace: Making the Most of Every Sample in Stochastic Trace Estimation\", SIAM J. Matrix Anal. Appl. 45(1), 2024 (arXiv:2301.07825).\nM. F. Hutchinson, \"A stochastic estimator of the trace of the influence matrix for Laplacian smoothing splines\", Comm. Statist. Simulation Comput. 18(3), 1989; A. Girard, \"A fast Monte-Carlo cross-validation procedure for large least squares problems with noisy data\", Numer. Math. 56, 1989.\n\n\n\n\n\n","title":"MatrixFreeRandomizedLinearAlgebra.trace"},{"category":"section","location":"examples/#Examples","page":"Examples","text":"The examples/ directory has runnable scripts that produce the figures below. They all use the same toy operator, a matrix-free 2D periodic Gaussian blur built from FFTs (see What \"matrix-free\" means). The matrix it stands for is n² × n² and is never formed, except for a small dense reference used for comparison.\n\nTo run them:\n\njulia --project=examples examples/01_rsvd_blur.jl\njulia --project=examples examples/02_reigen_hermitian.jl\njulia --project=examples examples/03_trace.jl\njulia --project=examples examples/04_performance.jl\n\nThe shared operator is in examples/operators.jl:\n\n# A matrix-free blur: forward and adjoint are each two FFTs.\nfunction blur_operator(n; sigma = n / 16, shift = (0, 0))\n    khat = fft(gaussian_psf(n; sigma = sigma, shift = shift))   # transfer function\n    N = n * n\n    fwd(x) = vec(real(ifft(khat .* fft(reshape(x, n, n)))))\n    adj(y) = vec(real(ifft(conj.(khat) .* fft(reshape(y, n, n)))))\n    return LinearMap{Float64}(fwd, adj, N, N)\nend","title":"Examples"},{"category":"section","location":"examples/#Randomized-SVD","page":"Examples","text":"examples/01_rsvd_blur.jl runs rsvd on a slightly asymmetric blur operator and compares against a dense svd. The recovered singular values land right on the exact spectrum, and the rank-r approximation error stays close to the optimal σᵣ₊₁.\n\nA = blur_operator(n; sigma = n / 10, shift = (1, 0))\nF = rsvd(A, k; num_oversamples = 40, num_power_iterations = 8, sample_vec = Float64[])\n# F.U * Diagonal(F.S) * F.Vt ≈ A\n\n(Image: Singular-value spectrum: randomized vs exact)\n\n(Image: Low-rank approximation error vs target rank)","title":"Randomized SVD"},{"category":"section","location":"examples/#Randomized-Hermitian-eigenvalues","page":"Examples","text":"examples/02_reigen_hermitian.jl uses the symmetric (zero-shift) blur, which is Hermitian positive semidefinite, and recovers its leading eigenvalues with reigvals_hermitian, comparing against a dense eigvals.\n\nA = blur_operator(n; sigma = n / 10) # Hermitian PSD\nλ = reigvals_hermitian(A, k; num_oversamples = 40, num_power_iterations = 8, sample_vec = Float64[])\n\n(Image: Leading eigenvalues: randomized vs exact)\n\nThe second figure shows the accuracy knob at work. With no power iteration the leading eigenvalues are already reasonable, but the estimates get worse toward the truncation rank. A few power iterations pull the whole curve down close to machine precision, at the cost of extra passes over the operator.\n\n(Image: Effect of power iterations on per-eigenvalue accuracy)","title":"Randomized Hermitian eigenvalues"},{"category":"section","location":"examples/#Trace-estimation","page":"Examples","text":"examples/03_trace.jl estimates tr(A) with both estimators and plots their relative error against the number of test vectors, averaged over many trials. XTrace's leave-one-out correction lets it drop well below the 1/√m rate that the streaming Hutchinson estimator follows.\n\nA = blur_operator(n; sigma = n / 20)\nt  = trace(A, 100; sample_vec = Float64[]) # XTrace, fixed budget\nt2 = trace(A, 100; low_mem = true, sample_vec = Float64[]) # streaming Hutchinson\nres = trace(A; relative_tolerance = 1e-2, return_error = true, sample_vec = Float64[])\n\n(Image: Trace estimation convergence: XTrace vs Hutchinson)","title":"Trace estimation"},{"category":"section","location":"examples/#Performance-vs-a-dense-factorization","page":"Examples","text":"examples/04_performance.jl times recovering the leading singular values two ways: materialize the operator and call dense svdvals, or call rsvdvals on the matrix-free operator. The dense route is cubic in the operator dimension and needs the whole matrix in memory, so it only runs for the smaller sizes. The matrix-free route keeps going well past the point where forming the matrix stops being practical.\n\n(Image: Runtime: dense vs matrix-free)\n\nThis is the situation the package is for. When the operator is large and you only want a few components, working through matrix-vector products instead of a dense factorization is what keeps the problem tractable.","title":"Performance vs a dense factorization"},{"category":"section","location":"#MatrixFreeRandomizedLinearAlgebra.jl","page":"Home","text":"Randomized algorithms for the top singular values, eigenvalues, and the trace of a large linear operator, without building the matrix. If you can apply your operator to a vector (and, for some routines, its adjoint), these routines work on it: dense and sparse matrices, GPU arrays, LinearMaps.jl operators, or anything else that defines size and *.\n\nThese are approximate, randomized methods of the kind described by Halko, Martinsson, and Tropp. They are most useful when the operator is large, a matrix-vector product is cheap, and you only want a low-rank piece of it. That is the case where a dense factorization is too slow or won't fit in memory.\n\n(Image: Runtime: dense vs matrix-free)","title":"MatrixFreeRandomizedLinearAlgebra.jl"},{"category":"section","location":"#What's-here","page":"Home","text":"rsvd / rsvdvals: randomized SVD and singular values for general (possibly rectangular) operators.\nreigen_hermitian / reigvals_hermitian: randomized eigendecomposition and eigenvalues for Hermitian operators.\ntrace: stochastic trace estimation. XTrace by default, or a streaming Hutchinson estimator when memory is tight.\n\nEverything runs on CPU or GPU arrays.","title":"What's here"},{"category":"section","location":"#Installation","page":"Home","text":"] add MatrixFreeRandomizedLinearAlgebra","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"using MatrixFreeRandomizedLinearAlgebra, LinearAlgebra\n\nA = randn(100, 50)            # any operator with size, *, and '\nU, S, Vt = rsvd(A, 10)        # rank-10 randomized SVD\nrel = opnorm(A - U * Diagonal(S) * Vt) / opnorm(A)\nprintln(\"relative error: \", rel)","title":"Quick start"},{"category":"section","location":"#Where-to-next","page":"Home","text":"What \"matrix-free\" means: the idea, and where matrix-free operators come from in Julia.\nAlgorithms: how each routine works.\nExamples: worked examples with accuracy and performance comparisons.\nAPI Reference: the full docstrings.\nReferences: the papers these implementations come from.","title":"Where to next"},{"category":"section","location":"matrix_free/#What-\"matrix-free\"-means","page":"What is matrix-free?","text":"Usually, when you do numerical linear algebra, you have matrix you can index into. That is, normally, you have an array of numbers sitting in memory. A matrix-free operator is the opposite. You never store the entries, you only know how to apply the operator to a vector. As long as you can compute y = A * x (and, for some algorithms, A' * x), the routines in this package never need A written out as a grid of numbers.\n\nThis matters when the matrix would be huge but its action is cheap. An n × n matrix needs O(n²) storage, and a dense multiply costs O(n²) work per vector. Many operators that come up in practice can be applied much faster than that, and often can't be stored at all at the sizes people care about.","title":"What \"matrix-free\" means"},{"category":"section","location":"matrix_free/#The-FFT","page":"What is matrix-free?","text":"A discrete Fourier transform is a dense n × n matrix (the DFT matrix), so applying it looks like it should cost O(n²). The fast Fourier transform gets the same answer in O(n log n) without ever forming that matrix. That is the idea in a nutshell: the operator has a perfectly good matrix representation, but you would never want to build it, so you apply it with an algorithm instead.\n\nThe examples in this documentation are built around this for matrix-free operations. A 2D Gaussian blur is a convolution, and convolution is diagonalized by the Fourier transform, so the blur operator is \"apply a forward FFT, multiply by the transfer function, apply an inverse FFT\". The matrix it stands for is n² × n², far too big to store for a reasonable image, but each apply is just a couple of FFTs.","title":"The FFT"},{"category":"section","location":"matrix_free/#Matrix-free-operators-in-Julia","page":"What is matrix-free?","text":"Julia makes this comfortable because functions and */mul! are first class. Some common sources of matrix-free operators:\n\nLinearMaps.jl wraps a function (or composes existing maps) into something that behaves like a matrix under *, ', size, and mul!. This is what the examples here use.\nLinearOperators.jl is a similar abstraction common in the optimization world.\nAbstractFFTs.jl and FFTW.jl: FFT plans are matrix-free operators you apply with *.\nPlain functions, sparse matrices, and GPU arrays work too, as long as they support size and the multiplies.","title":"Matrix-free operators in Julia"},{"category":"section","location":"matrix_free/#What-this-package-needs-from-your-operator","page":"What is matrix-free?","text":"The requirements are light. An operator has to support:\n\nsize(operator) for its dimensions,\noperator * X to multiply by a vector or a tall, skinny matrix,\noperator' * X for the adjoint multiply. rsvd and the XTrace estimator need this; the Hermitian eigen routines only use operator * X.\n\nA dense Matrix, a SparseMatrixCSC, a CuMatrix, a LinearMaps.LinearMap, or your own type all qualify. If the temporaries need to live on a particular device (CPU or GPU), pass a prototype array through the sample_vec keyword. For a LinearMap backed by CPU FFTs, for example, the examples pass sample_vec = Float64[].","title":"What this package needs from your operator"},{"category":"section","location":"algorithms/#Algorithms","page":"Algorithms","text":"This page describes, at a high level, what each routine does and where it comes from. It is meant for intuition. For the details, see the References.","title":"Algorithms"},{"category":"section","location":"algorithms/#The-randomized-range-finder","page":"Algorithms","text":"Every decomposition here is built on the same idea, from Halko, Martinsson, and Tropp. Suppose A is well approximated by something of rank k. If you multiply A by a tall, skinny matrix Ω whose columns are random Gaussian vectors, the result Y = A * Ω mixes the columns of A together, and with high probability its columns span most of the part of A's range that matters. Orthonormalizing Y with a thin QR factorization gives a matrix Q whose columns are an approximate basis for the range of A. Everything after that happens inside that small subspace, where the linear algebra is cheap.\n\nTwo knobs control how good Q is:\n\nOversampling (num_oversamples). Instead of exactly k random columns you draw k + p. The extra columns make it unlikely that the random sketch misses an important direction. A modest p (the default uses p = k) buys a lot of robustness.\nPower iterations (num_power_iterations). When the singular values decay slowly, the plain sketch is contaminated by the trailing part of the spectrum. Replacing A with (A A')^q A for a small q pushes the unwanted directions down without changing the leading subspace. Each power iteration costs a couple more passes over the operator, so it trades accuracy for work.\n\nThe routines pick reasonable defaults for both, and you can override them.","title":"The randomized range finder"},{"category":"section","location":"algorithms/#Randomized-SVD:-[rsvd](@ref),-[rsvdvals](@ref)","page":"Algorithms","text":"Once you have the range basis Q, the rest is small and exact. Project the operator into the subspace with B = Q' * A, take an ordinary SVD of the small matrix B, and lift the left singular vectors back with Q. The result is a rank-k SVD A ≈ U Σ Vᵀ, returned as a standard LinearAlgebra.SVD. rsvdvals does the same but skips the singular vectors when you only need the values. Tall operators (more rows than columns) are transposed internally so the sketch stays small.","title":"Randomized SVD: rsvd, rsvdvals"},{"category":"section","location":"algorithms/#Randomized-Hermitian-eigendecomposition:-[reigen_hermitian](@ref),-[reigvals_hermitian](@ref)","page":"Algorithms","text":"For a Hermitian operator the range finder also finds an invariant subspace, and you can use a cheaper one-sided power iteration since A is its own adjoint. Form the small projected matrix B = Q' * A * Q, eigendecompose it, and lift the eigenvectors back with Q. The leading eigenpairs come out sorted in descending order as a LinearAlgebra.Eigen. Eigenvalues need cleaner subspace separation than singular values, so the defaults here use more power iterations than rsvd.","title":"Randomized Hermitian eigendecomposition: reigen_hermitian, reigvals_hermitian"},{"category":"section","location":"algorithms/#Stochastic-trace-estimation:-[trace](@ref)","page":"Algorithms","text":"The trace of an operator you can only apply is estimated from samples of the form zᵀ A z. There are two estimators:\n\nGirard-Hutchinson (low_mem = true). Average zᵀ A z over random sign (Rademacher) vectors z. It is unbiased, holds only a vector or two at a time, and its error shrinks like 1/√m in the number of samples m. This is the fallback for operators so large that even a small sketch won't fit.\nXTrace (default). The estimator of Epperly, Tropp, and Webber. It pairs a small randomized low-rank sketch (the same range-finder idea) with a leave-one-out Hutchinson correction, so it gets a lot more out of each matrix-vector product when the operator has any spectral decay. It needs a bit more memory, since it keeps the sketch around, but it usually reaches a given accuracy with far fewer applies.\n\nEither estimator can run with a fixed sample budget or adaptively until it hits a target relative_tolerance, and either can return an error estimate alongside the value (return_error = true).","title":"Stochastic trace estimation: trace"},{"category":"section","location":"algorithms/#A-note-on-accuracy","page":"Algorithms","text":"These are randomized, approximate algorithms. They do not return the exact decomposition a dense solver would. They return a high-probability approximation whose accuracy you trade against cost through oversampling, power iterations, and the sample budget. What you get in return is that the work scales with the target rank and the cost of a matrix-vector product, not with the size of the dense matrix. That is what makes them worth using on large, matrix-free operators where a dense factorization isn't an option. The Examples page shows both sides: how close the approximations land next to a dense reference, and how the runtime pulls ahead as the operator grows.","title":"A note on accuracy"}]
}
